git init
// For creating git initializingkjansdknasdknaskd

git add "Filename"

git commit -a -m "Message"

git reset commitid
// It will only remove the commit

git reset --hard commitid
// it will bring it back to its back id position and remove the commit

git branch
// HEHE
// SHows list of branches..Current branch color will be green


git checkout -b nameofbranchtobebuilt
// Makes a new branch...Makes copy of the previous selected branch

git checkout nameofbranch
// Switches to that branch

git merge branchname
// It will merge branch with master

git remote add origin "Path"
// Identifies your path to the repository

git push -u origin nameofthebranchtobepushed
// Origin me bhi same naam hga aur saari changes push hojaen gi (-u means)

git pull
// Make changes from remote to local to all the code of remote...all the branches

git pull origin branchname
// Makes changes from remote to local to all the code of that branch

git stash
// saves your code.if u dont want to commit or add or to add in the history

git stash pop
// resumes your code. makes changes that u made earlier and saved it in git stash

git stash list
// gives u the history of ur work saved





